{
	"name": "uglify-css",
	"version": "0.0.1",
	"bin": "bin/uglify-css.js",
	"type": "module",
	"scripts": {
		"test:angular": "npm --prefix tests/angular run build && npm --prefix tests/angular run uglify -- -d",
		"test:vue": "npm --prefix tests/vue run build && npm --prefix tests/vue run uglify -- -d"
	},
	"dependencies": {
		"glob": "11.0.2"
	},
	"devDependencies": {
		"prettier": "3.5.3",
		"prettier-plugin-tailwindcss": "0.6.11"
	}
}
